fastapi
uvicorn

ultralytics==8.3.248   # ← Colab과 반드시 동일

numpy>=1.23,<2.0       # ← YOLO + opencv + torch 안전 범위
opencv-python-headless # ← 버전 고정 제거 (중요)
Pillow

albumentations
segmentation-models-pytorch
python-multipart

torch==2.0.1+cpu
torchvision==0.15.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu
